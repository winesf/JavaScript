Задачи на понимание основ JS (с реальных собеседований)

Вопросы к этому заданию
1.Какое будет выведено значение: let x = 5; alert( x++ ); ?
О. 5
Cначала выполняется функция потом выполняется инкрементация

2.Чему равно такое выражение: [ ] + false - null + true ?
О. NaN
Значение NaN генерируются арифметическими операциями, результатом которых является undefined или unrepresentable.
Такие условия не обязательно обозначают переполнение стека. NaN также может являться результатом попытки преобразования
числа в строку, или значения, не имеющего эквивалента в простых числовых примитивах.
Например, деление нуля на нуль возвращает NaN — но деление других чисел на 0 не возвращает NaN.

3.Что выведет этот код: let y = 1; let x = y = 2; alert(x); ?
О. Выведет 2 .

4.Чему равна сумма [ ] + 1 + 2?
О. 12 - Так как идет конкотенация .

5.Что выведет этот код: alert( "1"[0] )?
О. 1 - Мы обращаемся к нулевому символу в строке

6.Чему равно 2 && 1 && null && 0 && undefined ?
О. Null - оператор вернет первый ложное значение из списка. Или Последние верное.


7.Есть ли разница между выражениями? !!( a && b ) и (a && b)?
О. Есть заключается в типе возвращаемого значения. Если обе переменные a и b оба имеют логический тип,
 то эти выражения эквивалентны. В общем же случае, тип результата выражения (a && b) определяется типом операндов,
 тогда как выражение !!( a && b ) всегда возвращает значение логического типа.

8.Что выведет этот код: alert( null || 2 && 3 || 4 ); ?
О. 3
    1. 2 && 3 - приоритет оператора && выше , вернет тройку потому что && если опа операнда верны возращает последние верное
    2. null || 3 || 4  - вернет первое трушное .

9.a = [1, 2, 3]; b = [1, 2, 3]; Правда ли что a == b ?
О. Сравнение объекта истинно лишь в том случае, если оба операнда ссылаются на один и тот же объект в памяти.
   В данном случае это два разных объекта

10.Что выведет этот код: alert( +"Infinity" ); ?
О. Выведет число Infinity


11.Верно ли сравнение: "ёжик" > "яблоко"?
О. Да
Алгоритм сравнения двух строк довольно прост:

    Сначала сравниваются первые символы строк.
    Если первый символ первой строки больше (меньше), чем первый символ второй, то первая строка больше (меньше) второй.
    Если первые символы равны, то таким же образом сравниваются уже вторые символы строк.
    Сравнение продолжается, пока не закончится одна из строк.
    Если обе строки заканчиваются одновременно, то они равны. Иначе, большей считается более длинная строка.


12.Чему равно 0 || "" || 2 || undefined || true || falsе ?
O. 2 - потому что вернет первое трушное .
